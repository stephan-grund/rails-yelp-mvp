<%= simple_form_for([@restaurant, @review]) do |f| %>
  <%= f.input :content, label: "Write down your review", placeholder: "It was fantastic" %>>
  <%= f.input :rating, collection: %w[0 1 2 3 4 5] %>
  <%= f.submit class: "btn btn-primary", data: { disable_with: "Creating..." } %>
<% end %>
